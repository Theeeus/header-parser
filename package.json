{
  "name": "req-header-parser",
  "version": "1.0.0",
  "description": "full stack JavaScript app that gets IP address, language and OS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "engines": {
    "node": "6.9.1"
  }
}
